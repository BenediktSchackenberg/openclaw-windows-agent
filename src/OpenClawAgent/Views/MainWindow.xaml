<Window x:Class="OpenClawAgent.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:OpenClawAgent.ViewModels"
        mc:Ignorable="d"
        Title="OpenClaw Windows Agent" 
        Height="700" Width="1000"
        MinHeight="500" MinWidth="800"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource OpenClawWindowStyle}">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" Padding="20,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Logo & Title -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🦅" FontSize="28" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="OpenClaw Agent" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="{Binding ClientId, StringFormat='ID: {0}'}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                </StackPanel>

                <!-- Connection Status -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Style="{Binding ConnectionStatusStyle}" Margin="0,0,12,0">
                        <TextBlock Text="{Binding ConnectionStatusText}" FontSize="12" FontWeight="SemiBold" Foreground="White"/>
                    </Border>
                    <Button Content="⚙️" Style="{StaticResource SecondaryButtonStyle}" 
                            Command="{Binding OpenSettingsCommand}" ToolTip="Settings"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar Navigation -->
            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="NAVIGATION" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}" 
                               FontSize="11" Margin="0,0,0,12"/>
                    
                    <Button Content="📊 Dashboard" Style="{StaticResource SecondaryButtonStyle}" 
                            HorizontalContentAlignment="Left" Margin="0,0,0,4"
                            Command="{Binding NavigateToDashboardCommand}"/>
                    
                    <Button Content="🔗 Connector" Style="{StaticResource SecondaryButtonStyle}" 
                            HorizontalContentAlignment="Left" Margin="0,0,0,4"
                            Command="{Binding NavigateToConnectorCommand}"/>
                    
                    <Button Content="💬 Commands" Style="{StaticResource SecondaryButtonStyle}" 
                            HorizontalContentAlignment="Left" Margin="0,0,0,4"
                            Command="{Binding NavigateToCommandsCommand}"/>
                    
                    <Button Content="📋 Logs" Style="{StaticResource SecondaryButtonStyle}" 
                            HorizontalContentAlignment="Left" Margin="0,0,0,4"
                            Command="{Binding NavigateToLogsCommand}"/>
                </StackPanel>
            </Border>

            <!-- Content Area -->
            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="10,0,0,0">
                <ContentControl Content="{Binding CurrentView}"/>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" Padding="20,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" 
                           Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                
                <TextBlock Grid.Column="1" Text="{Binding GatewayLatency, StringFormat='Latency: {0}ms'}" 
                           Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="20,0"/>
                
                <TextBlock Grid.Column="2" Text="{Binding GatewayVersion, StringFormat='Gateway: {0}'}" 
                           Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="20,0"/>
                
                <TextBlock Grid.Column="3" Text="v0.2.0" 
                           Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>
