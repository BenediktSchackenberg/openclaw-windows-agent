<UserControl x:Class="OpenClawAgent.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Logs" FontSize="24" FontWeight="Bold" 
                       Foreground="{StaticResource TextPrimaryBrush}"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0"
                        Command="{Binding ToggleFollowCommand}">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="▶️ " Visibility="{Binding IsFollowing, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                            <TextBlock Text="⏸️ " Visibility="{Binding IsFollowing, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <TextBlock Text="Follow"/>
                        </StackPanel>
                    </Button.Content>
                </Button>
                <Button Content="📋 Copy" Style="{StaticResource SecondaryButtonStyle}" 
                        Command="{Binding CopyToClipboardCommand}" Margin="0,0,8,0"/>
                <Button Content="💾 Export" Style="{StaticResource SecondaryButtonStyle}" 
                        Command="{Binding ExportLogsCommand}" Margin="0,0,8,0"/>
                <Button Content="🗑️ Clear" Style="{StaticResource SecondaryButtonStyle}" 
                        Command="{Binding ClearLogsCommand}"/>
            </StackPanel>
        </Grid>

        <!-- Filters -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" CornerRadius="6" Padding="12" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Filter:" Foreground="{StaticResource TextSecondaryBrush}" 
                           VerticalAlignment="Center" Margin="0,0,8,0"/>

                <TextBox Grid.Column="1" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource OpenClawTextBoxStyle}"/>

                <TextBlock Grid.Column="2" Text="Level:" Foreground="{StaticResource TextSecondaryBrush}" 
                           VerticalAlignment="Center" Margin="16,0,8,0"/>

                <ComboBox Grid.Column="3" SelectedItem="{Binding FilterLevel}" Width="100">
                    <ComboBoxItem Content="All"/>
                    <ComboBoxItem Content="Debug"/>
                    <ComboBoxItem Content="Info"/>
                    <ComboBoxItem Content="Warning"/>
                    <ComboBoxItem Content="Error"/>
                </ComboBox>

                <CheckBox Grid.Column="5" Content="Auto-scroll" IsChecked="{Binding AutoScroll}" 
                          Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Log Output -->
        <Border Grid.Row="2" Background="#0D1117" CornerRadius="8" Padding="0">
            <ScrollViewer VerticalScrollBarVisibility="Auto" x:Name="LogScrollViewer">
                <ItemsControl ItemsSource="{Binding Logs}" Padding="12">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="70"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Timestamp -->
                                <TextBlock Text="{Binding Timestamp, StringFormat='{}{0:HH:mm:ss}'}" 
                                           FontFamily="{StaticResource MonoFont}" FontSize="12"
                                           Foreground="{StaticResource TextSecondaryBrush}"/>

                                <!-- Level -->
                                <Border Grid.Column="1" CornerRadius="3" Padding="4,1" 
                                        HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Level}" Value="Error">
                                                    <Setter Property="Background" Value="{StaticResource ErrorBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="Warning">
                                                    <Setter Property="Background" Value="{StaticResource WarningBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="Info">
                                                    <Setter Property="Background" Value="{StaticResource SuccessBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="Debug">
                                                    <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Level}" FontSize="10" FontWeight="SemiBold"
                                               Foreground="White"/>
                                </Border>

                                <!-- Source -->
                                <TextBlock Grid.Column="2" Text="{Binding Source}" 
                                           FontFamily="{StaticResource MonoFont}" FontSize="12"
                                           Foreground="{StaticResource PrimaryBrush}"/>

                                <!-- Message -->
                                <TextBlock Grid.Column="3" Text="{Binding Message}" TextWrapping="Wrap"
                                           FontFamily="{StaticResource MonoFont}" FontSize="12"
                                           Foreground="#C9D1D9"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
